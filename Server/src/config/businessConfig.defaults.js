// Defaults canónicos de BusinessConfig compartidos por persistencia, DTO read-only
// y cálculo de disponibilidad. No duplicar estos valores en consumidores.
export const DEFAULT_SLOT_DURATION_MINUTES = 60;
